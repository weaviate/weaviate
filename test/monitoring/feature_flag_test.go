package test

import (
	"testing"
)

func TestFeatureFlagEnabled(t *testing.T) {
	t.Parallel()
	// flag on, test Reset func
}

func TestFeatureFlagDisabled(t *testing.T) {
	t.Parallel()
	// flag off, test Reset func
}
