package network

import (
	"github.com/graphql-go/graphql"
)

// ResolveGraphQLRequest resolves the Network part of a GQL request
func ResolveGraphQLRequest(request graphql.ResolveParams) (interface{}, error) {
	return nil, nil
}
