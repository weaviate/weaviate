# TODO look over this config more
server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"
        http:
          endpoint: "0.0.0.0:4318"

ingester:
  max_block_bytes: 1_000_000
  max_block_duration: 5m

compactor:
  compaction:
    block_retention: 1h

# TODO do we want this?
# metrics_generator:
#   registry:
#     external_labels:
#       source: tempo
#       cluster: docker-compose
#   storage:
#     path: /tmp/tempo/generator/wal
#     remote_write:
#       - url: http://prometheus:9090/api/v1/write
#         send_exemplars: true

# TODO S3 storage configuration or can we just use remote write?
# https://grafana.com/docs/tempo/latest/configuration/hosted-storage/s3/
# storage:
#   trace:
#     backend: s3
#     s3:
#       bucket: tempo-s3
#       endpoint: tempo-s3:9000
#       access_key: aws_access_key
#       secret_key: aws_secret_key
#       force_path_style: true
#       region: us-east-1
#     wal:
#       path: /tmp/tempo/wal

# Local storage configuration
storage:
  trace:
    backend: local
    wal:
      path: /tmp/tempo/wal
    local:
      path: /tmp/tempo/blocks

# overrides:
#   defaults:
#     metrics_generator:
#       processors: [service-graphs, span-metrics] 