package graphqlapi

// Test with only local schema

// Test with one remote peer with independent schema

// Test with one remote peer with same schema

// Test with with two peers with different schema than local, but same on both remotes
